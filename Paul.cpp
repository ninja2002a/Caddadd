#include <iostream>

using namespace std;
//alt + x + linum-mode
//palidrome is a thing that does thing, example: doggod = palidrome, apple = not palidrome
//No more words than 80

int main() {

char str[80];
 cin.get(
char input;


 cout << str << endl;

if (input == " " || input.equals("")) {
 }

//checks for punctuations and spaces
for (int i = 0; i < sizeof(str); i++) {
  
 }
 
 return 0;
}
